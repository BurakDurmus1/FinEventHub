{
  "RabbitMq": {
    "Host": "localhost",
    "Port": 5672,
    "Username": "guest",
    "Password": "guest",

    "QueueName": "events",

    "RetryQueueName": "events.retry",

    "DeadLetterQueueName": "events.dlq",

    "RetryDelayMilliseconds": 5000,

    "MaxRetryCount": 3
  },

  "ConnectionStrings": {
    "Postgres": "Host=localhost;Port=5432;Database=FinEventHubDb;Username=postgres;Password=postgres"
  },

  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },

  "AllowedHosts": "*"
}